var name = 'Kuddus';
function add (num1, num2) {
    const result = num1 + num2;
    console.log('name inside', name);
    return result;
    function double (num) {
        return + 2;
    }
    var total = double(result);
    
}

console.log(name);

const plus = add(15, 14);
console.log(plus);